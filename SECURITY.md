# 安全策略

## 支持范围

安全修复优先提供给最新发布版本和 `main` 分支。历史版本不保证持续获得补丁，部署方应及时升级并关注发布说明。

## 报告漏洞

请不要为未修复漏洞创建公开 Issue，也不要公开可直接利用的复现代码、密钥或用户数据。请通过 GitHub Security Advisory 的“Report a vulnerability”私密报告入口联系维护者；如果仓库尚未启用该功能，请联系 `yangjian102621@163.com`，邮件标题以 `[GeekAI-Agent Security]` 开头。

报告建议包含：

- 受影响版本、组件和部署方式
- 漏洞影响与必要前置条件
- 最小复现步骤或概念验证
- 建议修复方案（如有）

维护者会尽快确认收到报告，并在复现、修复和发布补丁后协调披露时间。请在修复发布前保持信息私密。

## 部署方责任

- 首次部署必须设置独立的数据库、Redis、会话和管理员强密码
- 不向公网暴露 MySQL、Redis 或内部 API 端口
- 使用 HTTPS，并验证支付及第三方回调签名
- 定期轮换第三方 API 密钥并备份数据库和上传文件
- 不使用初始化示例值部署生产环境

任何疑似泄露的密钥都应立即在对应服务商处吊销并重新生成；仅从 Git 历史删除不能使密钥失效。
